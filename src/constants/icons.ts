import LIGHT_MODE from '@svgs/light-mode.svg';
import DARK_MODE from '@svgs/dark-mode.svg';

//lightIcon---------------------------------------
export const lightIcons = {
  lightDarkMode: LIGHT_MODE,
};

//darkIcon----------------------------------------
export const darkIcons = {
  lightDarkMode: DARK_MODE,
};
